# スクレイパー + R2保存 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** navi-idol.com 全アイドルをスクレイピングし Cloudflare R2 に保存、その後 D1 に投入できる状態にする

**Architecture:** Playwright で Vercel Security Checkpoint を突破してHTML取得、cheerio でパース、AWS SDK(S3互換)で R2 にアップロード。リスト収集→詳細取得→R2保存と、R2→D1インポートを2スクリプトに分離する。

**Tech Stack:** Playwright, cheerio, @aws-sdk/client-s3, drizzle-orm/d1-http, bun test

---

## ファイルマップ

| パス                                                                | 役割                                   |
| ------------------------------------------------------------------- | -------------------------------------- |
| `packages/scraper/package.json`                                     | パッケージ定義                         |
| `packages/scraper/tsconfig.json`                                    | 型設定（Workers型を除外）              |
| `packages/scraper/.env.example`                                     | 必要な環境変数の雛形                   |
| `packages/scraper/src/r2.ts`                                        | R2アップロード・取得ユーティリティ     |
| `packages/scraper/src/scrape/browser.ts`                            | Playwright ブラウザ管理                |
| `packages/scraper/src/scrape/parsers/idol-list.ts`                  | 一覧ページ→アイドルURL抽出             |
| `packages/scraper/src/scrape/parsers/idol-detail.ts`                | 詳細ページ→名前・グループ・画像URL抽出 |
| `packages/scraper/src/scrape/parsers/__tests__/idol-list.test.ts`   | idol-list ユニットテスト               |
| `packages/scraper/src/scrape/parsers/__tests__/idol-detail.test.ts` | idol-detail ユニットテスト             |
| `packages/scraper/src/scrape/parsers/__fixtures__/idol-list.html`   | テスト用HTMLフィクスチャ               |
| `packages/scraper/src/scrape/parsers/__fixtures__/idol-detail.html` | テスト用HTMLフィクスチャ               |
| `packages/scraper/src/scrape/main.ts`                               | スクレイピングメインスクリプト         |
| `packages/scraper/src/import-to-db/main.ts`                         | R2 → D1 インポートスクリプト           |
| `packages/infra/alchemy.run.ts`                                     | R2バケット追加                         |

**R2 キー構造（1バケット `oshi-idol-assets`）:**

```
idols/{naviIdolId}/profile.json   # 名前・グループ・画像key一覧
idols/{naviIdolId}/images/0.webp  # 画像バイナリ
idols/{naviIdolId}/images/1.webp
```

---

## Task 1: packages/scraper パッケージのセットアップ

**Files:**

- Create: `packages/scraper/package.json`
- Create: `packages/scraper/tsconfig.json`
- Create: `packages/scraper/.env.example`

- [ ] **Step 1: package.json を作成する**

```json
{
  "name": "@oshi-idol/scraper",
  "private": true,
  "type": "module",
  "scripts": {
    "scrape": "bun run src/scrape/main.ts",
    "import-to-db": "bun run src/import-to-db/main.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.750.0",
    "@oshi-idol/db": "workspace:*",
    "cheerio": "^1.0.0",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@oshi-idol/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
```

- [ ] **Step 2: tsconfig.json を作成する**

`@cloudflare/workers-types` はローカルスクリプトでは不要なので除外する。

```json
{
  "extends": "@oshi-idol/config/tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "outDir": "dist"
  },
  "include": ["src"]
}
```

- [ ] **Step 3: .env.example を作成する**

```
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=oshi-idol-assets
R2_PUBLIC_URL=https://pub-xxx.r2.dev

CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
```

- [ ] **Step 4: 依存パッケージをインストールし、Playwright ブラウザをダウンロードする**

```bash
bun install
bunx playwright install chromium
```

Expected: エラーなく完了すること

- [ ] **Step 5: コミット**

```bash
git add packages/scraper/
git commit -m "feat: @oshi-idol/scraper パッケージの骨格を追加"
```

---

## Task 2: infra に R2 バケットを追加

**Files:**

- Modify: `packages/infra/alchemy.run.ts`

- [ ] **Step 1: alchemy.run.ts に R2Bucket を追加する**

```ts
import alchemy from "alchemy";
import { D1Database, R2Bucket, TanStackStart } from "alchemy/cloudflare";
import { config } from "dotenv";

config({ path: "./.env" });
config({ path: "../../apps/web/.env" });

const app = await alchemy("oshi-idol");

const db = await D1Database("database", {
  migrationsDir: "../../packages/db/src/migrations",
});

const assets = await R2Bucket("assets", {
  name: "oshi-idol-assets",
});

export const web = await TanStackStart("web", {
  cwd: "../../apps/web",
  bindings: {
    DB: db,
    ASSETS: assets,
    CORS_ORIGIN: alchemy.env.CORS_ORIGIN!,
    BETTER_AUTH_SECRET: alchemy.secret.env.BETTER_AUTH_SECRET!,
    BETTER_AUTH_URL: alchemy.env.BETTER_AUTH_URL!,
  },
});

console.log(`Web    -> ${web.url}`);

await app.finalize();
```

- [ ] **Step 2: alchemy deploy を実行して R2 バケットを作成する**

```bash
cd packages/infra && bun run deploy
```

Expected: `oshi-idol-assets` バケットが Cloudflare 上に作成されること

> **補足**: デプロイ後、Cloudflare Dashboard → R2 でバケットの "Public Access" を有効化し、R2_PUBLIC_URL を取得すること

- [ ] **Step 3: コミット**

```bash
git add packages/infra/alchemy.run.ts
git commit -m "feat: infra に R2 バケット (oshi-idol-assets) を追加"
```

---

## Task 3: r2.ts — R2 ユーティリティ

**Files:**

- Create: `packages/scraper/src/r2.ts`

- [ ] **Step 1: src/r2.ts を作成する**

```ts
import {
  GetObjectCommand,
  ListObjectsV2Command,
  PutObjectCommand,
  S3Client,
} from "@aws-sdk/client-s3";
import dotenv from "dotenv";

dotenv.config();

const client = new S3Client({
  region: "auto",
  endpoint: `https://${process.env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: process.env.R2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
  },
});

const BUCKET = process.env.R2_BUCKET_NAME!;

export async function uploadBuffer(
  key: string,
  buffer: Buffer,
  contentType: string,
): Promise<void> {
  await client.send(
    new PutObjectCommand({
      Bucket: BUCKET,
      Key: key,
      Body: buffer,
      ContentType: contentType,
    }),
  );
}

export async function uploadJson(key: string, data: unknown): Promise<void> {
  await uploadBuffer(key, Buffer.from(JSON.stringify(data, null, 2)), "application/json");
}

export async function listKeys(prefix: string): Promise<string[]> {
  const keys: string[] = [];
  let continuationToken: string | undefined;

  do {
    const result = await client.send(
      new ListObjectsV2Command({
        Bucket: BUCKET,
        Prefix: prefix,
        ContinuationToken: continuationToken,
      }),
    );
    for (const obj of result.Contents ?? []) {
      if (obj.Key) keys.push(obj.Key);
    }
    continuationToken = result.NextContinuationToken;
  } while (continuationToken);

  return keys;
}

export async function getJson<T>(key: string): Promise<T> {
  const result = await client.send(new GetObjectCommand({ Bucket: BUCKET, Key: key }));
  const body = await result.Body?.transformToString();
  if (!body) throw new Error(`Empty body for key: ${key}`);
  return JSON.parse(body) as T;
}
```

- [ ] **Step 2: コミット**

```bash
git add packages/scraper/src/r2.ts
git commit -m "feat: R2 ユーティリティ (uploadBuffer/uploadJson/listKeys/getJson)"
```

---

## Task 4: browser.ts — Playwright ブラウザ管理

**Files:**

- Create: `packages/scraper/src/scrape/browser.ts`

- [ ] **Step 1: src/scrape/browser.ts を作成する**

```ts
import { chromium, type Browser } from "playwright";

export async function createBrowser(): Promise<Browser> {
  return chromium.launch({ headless: true });
}

export async function fetchRenderedHtml(browser: Browser, url: string): Promise<string> {
  const page = await browser.newPage();
  try {
    await page.goto(url, { waitUntil: "networkidle", timeout: 60_000 });
    // Vercel Security Checkpoint を通過するまで待機
    await page.waitForFunction(() => !document.title.includes("Checkpoint"), { timeout: 30_000 });
    await page.waitForLoadState("networkidle");
    return page.content();
  } finally {
    await page.close();
  }
}
```

- [ ] **Step 2: コミット**

```bash
git add packages/scraper/src/scrape/browser.ts
git commit -m "feat: Playwright ブラウザ管理 (fetchRenderedHtml)"
```

---

## Task 5: テスト用 HTML フィクスチャを作成する

**Files:**

- Create: `packages/scraper/src/scrape/parsers/__fixtures__/idol-list.html`
- Create: `packages/scraper/src/scrape/parsers/__fixtures__/idol-detail.html`

navi-idol.com の実際の HTML 構造（Web Archive で確認済み）に基づいたフィクスチャ。

- [ ] **Step 1: `__fixtures__/idol-list.html` を作成する**

```html
<!DOCTYPE html>
<html lang="ja">
  <body>
    <a
      class="block transition duration-300 ease-in-out hover:-translate-y-1 hover:opacity-80 hover:shadow-lg"
      href="/idol/abc12345"
    >
      <div class="relative z-10 h-5 flex">
        <div
          class="h-full flex items-center max-w-[70%] md:max-w-[50%] rounded-tl-lg w-36 bg-pink-light"
        >
          <span class="text-xs font-medium truncate pl-4">テストグループA</span>
        </div>
      </div>
      <div
        class="relative z-30 w-full rounded-tr-lg rounded-b-lg overflow-hidden p-2 md:p-4 rounded-tl-none bg-pink-dark"
      >
        <div class="overflow-hidden rounded-md"></div>
      </div>
    </a>
    <a
      class="text-center font-medium pt-1 md:pt-2 text-black text-sm md:text-lg transition duration-300 ease-in-out hover:opacity-50 hover:-translate-y-0.5"
      href="/idol/abc12345"
      >テストアイドルA</a
    >

    <a
      class="block transition duration-300 ease-in-out hover:-translate-y-1 hover:opacity-80 hover:shadow-lg"
      href="/idol/def67890"
    >
      <div class="relative z-10 h-5 flex">
        <div
          class="h-full flex items-center max-w-[70%] md:max-w-[50%] rounded-tl-lg w-36 bg-pink-light"
        >
          <span class="text-xs font-medium truncate pl-4">テストグループB</span>
        </div>
      </div>
      <div
        class="relative z-30 w-full rounded-tr-lg rounded-b-lg overflow-hidden p-2 md:p-4 rounded-tl-none bg-pink-dark"
      >
        <div class="overflow-hidden rounded-md"></div>
      </div>
    </a>
    <a
      class="text-center font-medium pt-1 md:pt-2 text-black text-sm md:text-lg transition duration-300 ease-in-out hover:opacity-50 hover:-translate-y-0.5"
      href="/idol/def67890"
      >テストアイドルB</a
    >
  </body>
</html>
```

- [ ] **Step 2: `__fixtures__/idol-detail.html` を作成する**

```html
<!DOCTYPE html>
<html lang="ja">
  <body>
    <h1 class="font-bold text-xl md:text-3xl text-left">テストアイドルA | テストグループA</h1>
    <img
      alt="テストアイドルA"
      loading="lazy"
      decoding="async"
      data-nimg="fill"
      class="z-[2] object-contain transition-all duration-500 ease-in-out"
      style="position:absolute;height:100%;width:100%;left:0;top:0;right:0;bottom:0;color:transparent"
      sizes="100vw"
      srcset="
        /_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fidol%2Ftest-uuid-0001.webp&amp;w=640   640w,
        /_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fidol%2Ftest-uuid-0001.webp&amp;w=1080 1080w
      "
    />
    <img
      alt="テストアイドルA"
      loading="lazy"
      decoding="async"
      data-nimg="fill"
      class="z-[2] object-contain transition-all duration-500 ease-in-out"
      style="position:absolute;height:100%;width:100%;left:0;top:0;right:0;bottom:0;color:transparent"
      sizes="100vw"
      srcset="
        /_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fidol%2Ftest-uuid-0002.webp&amp;w=640   640w,
        /_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fidol%2Ftest-uuid-0002.webp&amp;w=1080 1080w
      "
    />
    <img
      alt="グループ画像"
      loading="lazy"
      decoding="async"
      data-nimg="fill"
      class="z-[2] object-contain transition-all duration-500 ease-in-out"
      style="position:absolute;height:100%;width:100%;left:0;top:0;right:0;bottom:0;color:transparent"
      sizes="100vw"
      srcset="
        /_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fagent%2Fagent-img.webp&amp;w=640 640w
      "
    />
  </body>
</html>
```

> 注意: グループ画像 (`/images/agent/`) は idol の画像ではないためフィルタリングされる。フィクスチャにあえて含めることでテストで確認する。

- [ ] **Step 3: コミット**

```bash
git add packages/scraper/src/scrape/parsers/__fixtures__/
git commit -m "test: idol-list・idol-detail のHTMLフィクスチャを追加"
```

---

## Task 6: idol-list.ts パーサー（TDD）

**Files:**

- Create: `packages/scraper/src/scrape/parsers/__tests__/idol-list.test.ts`
- Create: `packages/scraper/src/scrape/parsers/idol-list.ts`

**サイト構造メモ:**

- 一覧ページURL: `https://navi-idol.com/idol?page=N`
- アイドルカード: `<a href="/idol/{id}">` （カード用・名前用の2つが同IDで出現）
- ID形式: `abc12345`（英数字8文字）

- [ ] **Step 1: 失敗するテストを書く**

`packages/scraper/src/scrape/parsers/__tests__/idol-list.test.ts`:

```ts
import { readFileSync } from "fs";
import { dirname, join } from "path";
import { fileURLToPath } from "url";
import { describe, expect, test } from "bun:test";

import { parseIdolList } from "../idol-list.ts";

const __dirname = dirname(fileURLToPath(import.meta.url));
const fixture = (file: string) => readFileSync(join(__dirname, "../__fixtures__", file), "utf-8");

describe("parseIdolList", () => {
  test("アイドルID・URLを重複なく抽出できる", () => {
    const result = parseIdolList(fixture("idol-list.html"));

    expect(result).toHaveLength(2);
    expect(result[0]).toEqual({
      naviIdolId: "abc12345",
      url: "https://navi-idol.com/idol/abc12345",
    });
    expect(result[1]).toEqual({
      naviIdolId: "def67890",
      url: "https://navi-idol.com/idol/def67890",
    });
  });

  test("同じIDが複数 a タグに現れても1件にまとまる", () => {
    const html = `
      <a href="/idol/xyz99999">card</a>
      <a href="/idol/xyz99999">name</a>
    `;
    const result = parseIdolList(html);
    expect(result).toHaveLength(1);
    expect(result[0]?.naviIdolId).toBe("xyz99999");
  });

  test("アイドルリンクが存在しないページでは空配列を返す", () => {
    const result = parseIdolList("<html><body><p>nothing here</p></body></html>");
    expect(result).toHaveLength(0);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd packages/scraper && bun test src/scrape/parsers/__tests__/idol-list.test.ts
```

Expected: `Cannot find module '../idol-list.ts'` のようなエラー

- [ ] **Step 3: idol-list.ts を実装する**

`packages/scraper/src/scrape/parsers/idol-list.ts`:

```ts
import * as cheerio from "cheerio";

export interface IdolListItem {
  naviIdolId: string;
  url: string;
}

export function parseIdolList(html: string): IdolListItem[] {
  const $ = cheerio.load(html);
  const items: IdolListItem[] = [];
  const seen = new Set<string>();

  $("a[href]").each((_, el) => {
    const href = $(el).attr("href") ?? "";
    const match = href.match(/\/idol\/([a-z0-9]+)$/);
    if (match?.[1] && !seen.has(match[1])) {
      seen.add(match[1]);
      items.push({
        naviIdolId: match[1],
        url: `https://navi-idol.com/idol/${match[1]}`,
      });
    }
  });

  return items;
}
```

- [ ] **Step 4: テストがパスすることを確認する**

```bash
cd packages/scraper && bun test src/scrape/parsers/__tests__/idol-list.test.ts
```

Expected: `3 tests passed`

- [ ] **Step 5: コミット**

```bash
git add packages/scraper/src/scrape/parsers/idol-list.ts packages/scraper/src/scrape/parsers/__tests__/idol-list.test.ts
git commit -m "feat: idol-list パーサー + テスト"
```

---

## Task 7: idol-detail.ts パーサー（TDD）

**Files:**

- Create: `packages/scraper/src/scrape/parsers/__tests__/idol-detail.test.ts`
- Create: `packages/scraper/src/scrape/parsers/idol-detail.ts`

**サイト構造メモ:**

- H1: `<h1 class="font-bold text-xl md:text-3xl text-left">名前 | グループ</h1>`
- 画像: `<img class="z-[2] ..." srcset="/_next/image?url=ENCODED_SUPABASE_URL&w=640 640w, ..."/>`
- `images/idol/` パスのみ対象（`images/agent/` や `images/footer/` は除外）

- [ ] **Step 1: 失敗するテストを書く**

`packages/scraper/src/scrape/parsers/__tests__/idol-detail.test.ts`:

```ts
import { readFileSync } from "fs";
import { dirname, join } from "path";
import { fileURLToPath } from "url";
import { describe, expect, test } from "bun:test";

import { parseIdolDetail } from "../idol-detail.ts";

const __dirname = dirname(fileURLToPath(import.meta.url));
const fixture = (file: string) => readFileSync(join(__dirname, "../__fixtures__", file), "utf-8");

describe("parseIdolDetail", () => {
  test("名前・グループを抽出できる", () => {
    const result = parseIdolDetail(fixture("idol-detail.html"));

    expect(result.name).toBe("テストアイドルA");
    expect(result.group).toBe("テストグループA");
  });

  test("idol 画像URLを抽出できる（agent画像は含まない）", () => {
    const result = parseIdolDetail(fixture("idol-detail.html"));

    expect(result.imageUrls).toHaveLength(2);
    expect(result.imageUrls[0]).toBe(
      "https://vbtjlshykdswgsmmoclj.supabase.co/storage/v1/object/public/images/idol/test-uuid-0001.webp",
    );
    expect(result.imageUrls[1]).toBe(
      "https://vbtjlshykdswgsmmoclj.supabase.co/storage/v1/object/public/images/idol/test-uuid-0002.webp",
    );
  });

  test("画像URLが重複しない", () => {
    const html = `
      <h1 class="font-bold text-xl md:text-3xl text-left">重複テスト | グループ</h1>
      <img class="z-[2] object-contain" srcset="/_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fidol%2Fdupe.webp&amp;w=640 640w, /_next/image?url=https%3A%2F%2Fvbtjlshykdswgsmmoclj.supabase.co%2Fstorage%2Fv1%2Fobject%2Fpublic%2Fimages%2Fidol%2Fdupe.webp&amp;w=1080 1080w"/>
    `;
    const result = parseIdolDetail(html);
    expect(result.imageUrls).toHaveLength(1);
  });

  test("| 区切りのないH1でも name に全文が入りgroup は空文字になる", () => {
    const html = `<h1 class="font-bold text-xl md:text-3xl text-left">ソロアイドル</h1>`;
    const result = parseIdolDetail(html);
    expect(result.name).toBe("ソロアイドル");
    expect(result.group).toBe("");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd packages/scraper && bun test src/scrape/parsers/__tests__/idol-detail.test.ts
```

Expected: `Cannot find module '../idol-detail.ts'` のようなエラー

- [ ] **Step 3: idol-detail.ts を実装する**

`packages/scraper/src/scrape/parsers/idol-detail.ts`:

```ts
import * as cheerio from "cheerio";

export interface IdolDetail {
  name: string;
  group: string;
  imageUrls: string[];
}

export function parseIdolDetail(html: string): IdolDetail {
  const $ = cheerio.load(html);

  const h1Text = $("h1").first().text().trim();
  const separatorIdx = h1Text.indexOf(" | ");
  const name = separatorIdx >= 0 ? h1Text.slice(0, separatorIdx).trim() : h1Text;
  const group = separatorIdx >= 0 ? h1Text.slice(separatorIdx + 3).trim() : "";

  const imageUrls: string[] = [];

  $("img[srcset]").each((_, el) => {
    const srcset = $(el).attr("srcset") ?? "";
    // Next.js image URL pattern: /_next/image?url=ENCODED_URL&w=...
    // srcset に複数解像度が含まれるため最初の url= のみ取得
    const match = srcset.match(/url=([^&\s,]+)/);
    if (!match?.[1]) return;

    const decoded = decodeURIComponent(match[1]);
    // /images/idol/ のみ対象（/images/agent/, /images/footer/ は除外）
    if (decoded.includes("/images/idol/")) {
      imageUrls.push(decoded);
    }
  });

  return {
    name,
    group,
    imageUrls: [...new Set(imageUrls)],
  };
}
```

- [ ] **Step 4: テストがパスすることを確認する**

```bash
cd packages/scraper && bun test src/scrape/parsers/__tests__/idol-detail.test.ts
```

Expected: `4 tests passed`

- [ ] **Step 5: ルートから全テストが通ることを確認する**

```bash
bun test
```

Expected: エラーなし

- [ ] **Step 6: コミット**

```bash
git add packages/scraper/src/scrape/parsers/idol-detail.ts packages/scraper/src/scrape/parsers/__tests__/idol-detail.test.ts
git commit -m "feat: idol-detail パーサー + テスト"
```

---

## Task 8: scrape/main.ts — メインスクレイピングスクリプト

**Files:**

- Create: `packages/scraper/src/scrape/main.ts`

- [ ] **Step 1: src/scrape/main.ts を作成する**

```ts
import { chromium } from "playwright";
import dotenv from "dotenv";

import { uploadBuffer, uploadJson } from "../r2.ts";
import { fetchRenderedHtml } from "./browser.ts";
import { parseIdolDetail } from "./parsers/idol-detail.ts";
import { parseIdolList } from "./parsers/idol-list.ts";

dotenv.config();

const BASE_URL = "https://navi-idol.com";
const DELAY_MS = 1500;

async function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

async function downloadImageBuffer(url: string): Promise<Buffer | null> {
  try {
    const res = await fetch(url);
    if (!res.ok) return null;
    return Buffer.from(await res.arrayBuffer());
  } catch {
    return null;
  }
}

async function main(): Promise<void> {
  const browser = await chromium.launch({ headless: true });

  try {
    // Phase 1: アイドルURL一覧を全ページから収集
    const allItems: Array<{ naviIdolId: string; url: string }> = [];
    let page = 0;

    while (true) {
      console.log(`Fetching list page ${page}...`);
      const html = await fetchRenderedHtml(browser, `${BASE_URL}/idol?page=${page}`);
      const items = parseIdolList(html);

      if (items.length === 0) {
        console.log(`Page ${page}: empty, stopping.`);
        break;
      }

      allItems.push(...items);
      console.log(`  +${items.length} idols (total: ${allItems.length})`);
      page++;
      await sleep(DELAY_MS);
    }

    // Phase 2: 各アイドルの詳細を取得して R2 に保存
    for (const [i, item] of allItems.entries()) {
      console.log(`\n[${i + 1}/${allItems.length}] ${item.url}`);

      try {
        const html = await fetchRenderedHtml(browser, item.url);
        const detail = parseIdolDetail(html);

        const savedImages: Array<{ key: string; originalUrl: string }> = [];

        for (const [imgIdx, imgUrl] of detail.imageUrls.entries()) {
          const buffer = await downloadImageBuffer(imgUrl);
          if (!buffer) {
            console.warn(`  Skipped image: ${imgUrl}`);
            continue;
          }

          const ext = imgUrl.endsWith(".webp") ? "webp" : "jpg";
          const key = `idols/${item.naviIdolId}/images/${imgIdx}.${ext}`;
          await uploadBuffer(key, buffer, `image/${ext}`);
          savedImages.push({ key, originalUrl: imgUrl });
          console.log(`  Uploaded image[${imgIdx}]`);
        }

        const profileKey = `idols/${item.naviIdolId}/profile.json`;
        await uploadJson(profileKey, {
          naviIdolId: item.naviIdolId,
          name: detail.name,
          group: detail.group,
          sourceUrl: item.url,
          scrapedAt: new Date().toISOString(),
          images: savedImages,
        });

        console.log(`  Saved: ${detail.name} (${detail.group}), ${savedImages.length} images`);
      } catch (err) {
        console.warn(`  Failed: ${item.url}`, err);
      }

      await sleep(DELAY_MS);
    }

    console.log("\nDone!");
  } finally {
    await browser.close();
  }
}

main().catch(console.error);
```

- [ ] **Step 2: 型チェックを通す**

```bash
cd packages/scraper && bun run check-types
```

Expected: エラーなし

- [ ] **Step 3: コミット**

```bash
git add packages/scraper/src/scrape/main.ts
git commit -m "feat: scrape/main.ts — 全アイドルを R2 に保存するスクレイパー"
```

---

## Task 9: import-to-db/main.ts — R2 → D1 インポート

**Files:**

- Create: `packages/scraper/src/import-to-db/main.ts`

**D1 接続の前提:**

- `.env` に `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_D1_DATABASE_ID`, `CLOUDFLARE_D1_TOKEN` を設定する
- D1 API Token は Cloudflare Dashboard → My Profile → API Tokens で "D1 Edit" 権限で発行する

- [ ] **Step 1: src/import-to-db/main.ts を作成する**

```ts
import dotenv from "dotenv";
import { and, eq } from "drizzle-orm";
import { drizzle } from "drizzle-orm/d1-http";

import { idolPhotos, idols } from "@oshi-idol/db/schema/idols";
import { getJson, listKeys } from "../r2.ts";

dotenv.config();

interface ProfileJson {
  naviIdolId: string;
  name: string;
  group: string;
  sourceUrl: string;
  scrapedAt: string;
  images: Array<{ key: string; originalUrl: string }>;
}

const db = drizzle({
  accountId: process.env.CLOUDFLARE_ACCOUNT_ID!,
  databaseId: process.env.CLOUDFLARE_D1_DATABASE_ID!,
  token: process.env.CLOUDFLARE_D1_TOKEN!,
});

const R2_PUBLIC_URL = process.env.R2_PUBLIC_URL!;

async function main(): Promise<void> {
  const allKeys = await listKeys("idols/");
  const profileKeys = allKeys.filter((k) => k.endsWith("/profile.json"));
  console.log(`Found ${profileKeys.length} profiles in R2`);

  for (const [i, key] of profileKeys.entries()) {
    const profile = await getJson<ProfileJson>(key);
    console.log(`\n[${i + 1}/${profileKeys.length}] ${profile.name} (${profile.group})`);

    try {
      // name + group で既存アイドルを検索
      const existing = await db
        .select()
        .from(idols)
        .where(and(eq(idols.name, profile.name), eq(idols.group, profile.group)))
        .limit(1);

      let idolId: string;

      if (existing.length > 0 && existing[0]) {
        idolId = existing[0].id;
        // 既存写真を削除してから再登録（上書きモード）
        await db.delete(idolPhotos).where(eq(idolPhotos.idolId, idolId));
        console.log(`  Updated: ${idolId}`);
      } else {
        const [inserted] = await db
          .insert(idols)
          .values({ name: profile.name, group: profile.group })
          .returning();
        if (!inserted) throw new Error("Failed to insert idol");
        idolId = inserted.id;
        console.log(`  Inserted: ${idolId}`);
      }

      if (profile.images.length > 0) {
        await db.insert(idolPhotos).values(
          profile.images.map((img, idx) => ({
            idolId,
            imageUrl: `${R2_PUBLIC_URL}/${img.key}`,
            sortOrder: idx,
          })),
        );
      }

      console.log(`  Photos: ${profile.images.length}`);
    } catch (err) {
      console.warn(`  Failed: ${profile.name}`, err);
    }
  }

  console.log("\nDone!");
}

main().catch(console.error);
```

- [ ] **Step 2: 型チェックを通す**

```bash
cd packages/scraper && bun run check-types
```

Expected: エラーなし

- [ ] **Step 3: ルートから全テストが通ることを確認する**

```bash
bun test
```

Expected: 全テストパス

- [ ] **Step 4: コミット**

```bash
git add packages/scraper/src/import-to-db/main.ts
git commit -m "feat: import-to-db/main.ts — R2 → D1 インポートスクリプト"
```

---

## 実行手順（完成後）

```bash
# 1. .env を設定する
cp packages/scraper/.env.example packages/scraper/.env
# （各値を埋める）

# 2. スクレイピング → R2 保存（数時間かかる場合あり）
pnpm --filter @oshi-idol/scraper scrape

# 3. R2 → D1 インポート
pnpm --filter @oshi-idol/scraper import-to-db
```
